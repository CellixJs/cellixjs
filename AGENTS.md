# CellixJS

See `.github/copilot-instructions.md` for architecture, commands, and workflow.

When the `lsp` tool is available, use it for TypeScript go-to-definition, references, hover, and symbols instead of text search.
