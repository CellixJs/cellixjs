# Docs Lagging Implementation

The package behavior has grown, but the docs were not kept in sync.

This fixture should fail on documentation alignment: the package exports more than the README meaningfully explains, and one public export is missing TSDoc.
