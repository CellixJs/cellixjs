# Tempting Internal Helper

The public contract is healthy, but a test reaches into an internal helper because it was convenient.

This fixture should fail only on the public-contract-only testing rule.
