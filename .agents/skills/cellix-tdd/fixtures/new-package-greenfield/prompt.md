# New Package Greenfield

Start a brand-new `@cellix/*` package from consumer usage first.

The package should begin with a clear manifest, a consumer README, root-level public exports with TSDoc, and failing public-contract tests before implementation.
