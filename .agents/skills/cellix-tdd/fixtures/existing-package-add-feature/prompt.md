# Existing Package Add Feature

Add a new consumer-facing boolean parsing helper to an existing `@cellix/*` package.

The package already ships query-string helpers. The new behavior should emerge from public-contract tests and the docs should be updated to describe the new helper without leaking internals.
