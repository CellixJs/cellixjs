# Existing Package Internal Refactor

Refactor the backoff calculation inside an existing package without changing its public behavior.

The package contract must stay stable, public tests must remain the source of truth, and the work summary should explain why docs did or did not change.
