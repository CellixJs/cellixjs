# Leaky Overbroad API

The package accidentally publishes an internal helper as a public subpath.

This fixture should fail when the evaluator inspects the export surface and the release hardening notes.
