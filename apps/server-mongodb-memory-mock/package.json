{
	"name": "@apps/server-mongodb-memory-mock",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"prebuild": "pnpm run lint",
		"build": "tsgo --build",
		"clean": "rimraf dist tsconfig.tsbuildinfo",
		"lint": "biome lint",
		"format": "biome format --write",
		"format:check": "biome format .",
		"start": "node dist/index.js",
		"dev": "cellix-local-dev tsx --profile mongo-memory-mock",
		"dev:worktree": "cellix-local-dev tsx --profile mongo-memory-mock"
	},
	"dependencies": {
		"@cellix/server-mongodb-memory-mock-seedwork": "workspace:*",
		"dotenv": "^16.4.5"
	},
	"devDependencies": {
		"@cellix/local-dev": "workspace:*",
		"@cellix/config-typescript": "workspace:*",
		"rimraf": "catalog:",
		"typescript": "catalog:"
	}
}
