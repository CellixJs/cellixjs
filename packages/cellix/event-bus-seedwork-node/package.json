{
  "name": "@cellix/event-bus-seedwork-node",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "files": ["dist"],
  "exports": {
    "./node-event-bus": {
      "types": "./dist/src/node-event-bus.d.ts",
      "default": "./dist/src/node-event-bus.js"
    },
    "./in-proc-event-bus": {
      "types": "./dist/src/in-proc-event-bus.d.ts",
      "default": "./dist/src/in-proc-event-bus.js"
    }
  },
  "scripts": {
    "prebuild": "biome lint",
    "build": "tsc --build",
    "watch": "tsc --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "biome lint",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@cellix/domain-seedwork": "workspace:*",
    "@opentelemetry/api": "^1.9.0"
  },
  "devDependencies": {
    "@cellix/typescript-config": "workspace:*",
    "@cellix/vitest-config": "workspace:*",
    "typescript": "catalog:",
    "rimraf": "^6.0.1"
  }
}
