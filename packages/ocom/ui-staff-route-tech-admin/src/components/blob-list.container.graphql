query BlobExplorerBlobs($input: BlobExplorerListBlobsInput!) {
	blobExplorerListBlobs(input: $input) {
		items {
			name
			contentType
			contentLength
			lastModified
			metadata
			tags
		}
		prefixes
		continuationToken
	}
}
