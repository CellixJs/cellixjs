{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "import-requests",
	"title": "ImportRequestMessage",
	"description": "Message for an import request to be processed",
	"type": "object",
	"properties": {
		"importId": {
			"type": "string",
			"description": "Unique identifier for the import request"
		},
		"requestedBy": {
			"type": "string",
			"description": "User ID who requested the import"
		},
		"fileUrl": {
			"type": "string",
			"description": "URL of the file to import"
		}
	},
	"required": ["importId", "requestedBy", "fileUrl"],
	"additionalProperties": false
}
