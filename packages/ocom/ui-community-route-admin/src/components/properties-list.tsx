import { PropertyList, type PropertyListProps } from '@ocom/ui-community-shared';

/** Manager adapter retaining the established Property list component API. */
export type PropertiesListProps = PropertyListProps;

export const PropertiesList = PropertyList;
