{
	"name": "@ocom/service-apollo-server",
	"version": "1.0.0",
	"author": "",
	"license": "MIT",
	"description": "Apollo Server infrastructure service for CellixJS",
	"type": "module",
	"main": "dist/src/index.js",
	"types": "dist/src/index.d.ts",
	"scripts": {
		"build": "tsc --build",
		"clean": "rimraf dist node_modules tsconfig.tsbuildinfo && tsc --build --clean",
		"lint": "biome lint",
		"format": "biome format --write",
        "test": "vitest run",
        "test:all": "npm run test",
        "test:coverage": "vitest run --coverage",
        "test:unit": "vitest run",
        "test:watch": "vitest watch",
		"prebuild": "biome lint"
	},
	"peerDependencies": {
		"@cellix/api-services-spec": "^1.0.0"
	},
	"dependencies": {
		"@apollo/server": "^4.11.3",
		"@azure/functions": "^4.6.0",
		"@graphql-tools/schema": "^10.0.8",
		"graphql": "^16.10.0",
		"graphql-middleware": "^6.1.35"
	},
	"devDependencies": {
		"rimraf": "^6.0.1",
		"typescript": "^5.8.3"
	}
}